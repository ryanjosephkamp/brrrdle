# Game storage helpers

This folder contains local browser-storage helpers for daily game sessions. These helpers persist in-progress `og` and `go` sessions without changing the stable `localStorage` keys used by existing players.
