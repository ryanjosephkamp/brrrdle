# App game route panels

This folder contains React route panels for playable `og` and `go` game modes. The UI components here compose shared game-engine logic from `src/game/`, data loading from `src/data/`, persistence from `src/game/storage/`, and reusable primitives from `src/ui/`.
