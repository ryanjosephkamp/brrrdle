# Public browser-safe Supabase configuration for future account/admin phases.
# Do not commit service-role keys or other privileged credentials.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional public app metadata.
VITE_APP_ENV=local
